package org.example.tpoprogramacioniii.controller;

import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

// BFS, DFS, Dijkstra

// BFS: busqueda del camino con la menor cantidad de paradas
// DFS: determinar si todos los nodos de la red estan conectados
// Dijkstra: calculo de la ruta mas corta/barata/rapida entre 2 ubicaciones (VER OptimizationCriteriaEnum)

@RestController
@RequestMapping("/api/location")
public class LocationController {
}
